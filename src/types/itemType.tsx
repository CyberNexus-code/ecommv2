export type ItemType = {
    id: string
    category_id: string
    name: string
    description: string
    price: number
    quantity: number
    is_active: boolean
    is_deleted: boolean
    created_at: string
    updated_at: string
}